name: refresh-dashboard
on:
  schedule:
    - cron: '0 5 * * 1-5'    # Пн–Пт 05:00 UTC = 08:00 МСК
    - cron: '0 17 * * 1-5'   # Пн–Пт 17:00 UTC = 20:00 МСК
  workflow_dispatch:           # можно запустить вручную кнопкой
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - name: Тянем Google -> считаем -> собираем файл
        env:
          GOOGLE_SA_JSON: ${{ secrets.GOOGLE_SA_JSON }}
        run: |
          python -m pipeline.gsync
          python -m pipeline.build
          python -m pipeline.publish
          mkdir -p _site
          cp dist/rezerv-dashboard.html _site/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deploy
        uses: actions/deploy-pages@v4
